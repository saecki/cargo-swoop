# cargo-swoop

A cli tool to find and remove target directories.

```
Usage: cargo-swoop [OPTIONS] [SEARCH_DIR]

Arguments:
  [SEARCH_DIR]

Options:
      --follow-symlinks
      --show-empty
  -h, --help             Print help
```
